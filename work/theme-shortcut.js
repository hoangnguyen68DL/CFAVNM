(()=>{const actions=document.querySelector('.cp-top-actions');if(!actions)return;const button=document.createElement('button');button.id='quickThemeToggle';button.type='button';button.className='btn ghost quick-theme-toggle';button.setAttribute('translate','no');actions.insertBefore(button,document.getElementById('cpSyncTop'));function update(){const dark=document.body.classList.contains('ux-dark');button.dataset.uxTheme=dark?'light':'dark';button.setAttribute('aria-label',dark?'Chuyển sang giao diện sáng':'Chuyển sang giao diện tối');button.title=button.getAttribute('aria-label');button.innerHTML=dark?'<span aria-hidden="true">☀</span><span>Sáng</span>':'<span aria-hidden="true">☾</span><span>Tối</span>';}new MutationObserver(update).observe(document.body,{attributes:true,attributeFilter:['class']});update()})();
