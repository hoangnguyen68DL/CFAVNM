const fs=require('fs'),vm=require('vm'),assert=require('assert/strict');const source=fs.readFileSync('work/study-preservation.js','utf8');const store=new Map([['cfa-timelog',JSON.stringify({'2026-09-12':0})],['cfa-sessions',JSON.stringify([{date:'2026-09-12',startHour:10,minutes:70},{date:'2026-09-12',startHour:12,minutes:35}])]]);const context={window:{},localStorage:{getItem:k=>store.get(k)||null,setItem:(k,v)=>store.set(k,v)}};vm.runInNewContext(source,context);assert.equal(JSON.parse(store.get('cfa-timelog'))['2026-09-12'],105);assert.equal(context.window.StudyPreservation.recoveredMinutes,105);assert([...store.keys()].some(k=>k.startsWith('charter-study-backup-')));vm.runInNewContext(source,context);assert.equal(JSON.parse(store.get('cfa-timelog'))['2026-09-12'],105);assert.equal(context.window.StudyPreservation.mergeTime({'2026-09-12':105},{'2026-09-12':10})['2026-09-12'],105);console.log('PASS: restore from saved sessions, backup before recovery, no duplicate credit, older remote cannot reduce local hours.');